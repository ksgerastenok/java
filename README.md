# java

This repository contains different java samples, such as Base64 encoder / decoder, Multithreaded compressor / decompressor, integer divider / remainder calculator based on substract operation with O(log n) complexity.
